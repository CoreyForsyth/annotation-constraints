plugins {
    id 'java'
}

dependencies {
    compileOnly project(":annotation-constraints-demo-annotation")
    annotationProcessor project(":annotation-constraints-demo-annotation")
}

test {
    useJUnitPlatform()
}