plugins {
    id 'java-library'
}

dependencies {
    compileOnly project(':annotation-constraints-processor')
    annotationProcessor project(':annotation-constraints-processor')
    implementation project(':annotation-constraints')
}

test {
    useJUnitPlatform()
}